// Entry point for consumers that only need the portal, exposed as
// `@foxy.io/inflow/portal` and `inflow@1/portal.js` on the CDN.
export { InflowPortal as Portal, type InflowPortalConfig } from "../portal";
